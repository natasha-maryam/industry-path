PROGRAM Main
(* Generated by CrossLayerX deterministic pipeline *)
VAR
    EquipmentManager : FB_EquipmentManager;
    LoopManager : FB_ControlLoopManager;
    InterlockManager : FB_InterlockManager;
    AlarmManager : FB_AlarmManager;
    StartupSequence : FB_StartupSequence;
    ShutdownSequence : FB_ShutdownSequence;
    StartupStepState : INT := 0;
    ShutdownStepState : INT := 0;
END_VAR

{% for note in notes %}(* {{ note }} *)
{% endfor %}
StartupSequence(Execute := STARTUP_CMD, StepState := StartupStepState);
EquipmentManager();
LoopManager();
InterlockManager();
AlarmManager();
ShutdownSequence(Execute := SHUTDOWN_CMD, StepState := ShutdownStepState);

END_PROGRAM
